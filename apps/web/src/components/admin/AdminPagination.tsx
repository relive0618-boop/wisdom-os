"use client"; export function AdminPagination({ page, total, onChange }: { page: number; total: number; onChange: (page: number) => void }) { return <div className="flex gap-2"><button disabled={page <= 1} onClick={() => onChange(page - 1)}>上一頁</button><span>{page} / {Math.max(total, 1)}</span><button disabled={page >= total} onClick={() => onChange(page + 1)}>下一頁</button></div>; }
