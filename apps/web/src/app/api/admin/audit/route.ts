import { NextResponse } from "next/server";
import { adminContext } from "@/lib/admin/server";
import { safeAuditMetadata } from "@/lib/admin/safeAuditMetadata";
export async function GET(request: Request) { const context = await adminContext(); if ("error" in context) return context.error; const query = new URL(request.url).searchParams; let requestQuery = context.client.from("admin_audit_logs").select("actor_id,action,entity_type,entity_id,metadata,created_at").order("created_at", { ascending: false }).range(Number(query.get("offset") ?? 0), Number(query.get("offset") ?? 0) + 49); if (query.get("action")) requestQuery = requestQuery.eq("action", query.get("action")!); if (query.get("entity_type")) requestQuery = requestQuery.eq("entity_type", query.get("entity_type")!); const { data, error } = await requestQuery; if (error) return NextResponse.json({ error: { code: "CLOUD_TEMPORARILY_UNAVAILABLE" } }, { status: 503 }); return NextResponse.json({ data: (data ?? []).map((item) => ({ actor: item.actor_id ? "authenticated-admin" : "system", action: item.action, entityType: item.entity_type, entityId: item.entity_id, createdAt: item.created_at, metadata: safeAuditMetadata(item.metadata) })) }); }
