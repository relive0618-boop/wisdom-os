import { conflictDuplicateId } from "@/lib/cloud/sync";
export type Resolution = "local" | "cloud" | "both" | "later";
export function resolveConflict<T extends { id?: string; reportId?: string; cycleId?: string }>(value: T, resolution: Resolution) { if (resolution === "later") return { action: "keep_conflict", value }; if (resolution === "cloud") return { action: "backup_then_replace", value, backup: { ...value, backupOf: value.reportId ?? value.cycleId ?? value.id, source: "conflict_backup", createdAt: new Date().toISOString() } }; if (resolution === "both") { const id = value.reportId ?? value.cycleId ?? value.id ?? "record"; return { action: "duplicate", value: { ...value, duplicatedFrom: id, reportId: value.reportId ? conflictDuplicateId(value.reportId) : value.reportId, cycleId: value.cycleId ? conflictDuplicateId(value.cycleId) : value.cycleId } }; } return { action: "update_with_expected_revision", value }; }
