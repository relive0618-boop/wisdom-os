# Wisdom OS v0.4 最終綜合安全審計

- 審計提交：`84811032d3b6ffa5d63dbfbc645d8ab6d3fddadf`
- 比對基線：`bb489326adaad39f6fca965894c3871cd1b8ae1e`
- 範圍：Draft PR #3 的程式、migration、權限、同步、Admin/Audit、限流、依賴、建置產物與 Protected Preview。
- 結論：未發現 P0 或目前可利用的 P1；可進入 Ready-for-review 判定。

## 已驗證邊界

- Git diff 無空白錯誤；PR 可合併，且沒有刪除既有功能。
- 追蹤檔案、PR diff、環境檔與建置後前端產物均未發現實際秘密、私鑰、資料庫連線、服務角色憑證或 API Key。前端僅包含設定頁刻意展示的環境變數名稱，未包含其值。
- Supabase 的 7 張資料表均啟用 RLS；匿名角色寫入授權為 0；瀏覽器角色無 `rate_limit_buckets` 授權；限流 RPC 僅可由 `service_role` 執行。
- SECURITY DEFINER 函式均使用固定 search path；Admin workflow/audit 的 4 個 trigger 已安裝；Audit metadata 未含 payload、token、secret、cookie 或 SQL。
- 資料基線：知識 56、案例 30、Audit 10；報告與 PDCA 均為 total 2 / active 1 / soft-deleted 1。限流資料無不合法列，五分鐘 Cron 清理工作存在。
- 同步採本機優先與明確使用者操作；還原不覆蓋同 ID 本機資料；雲端 API 以已驗證使用者宣告與 RLS 作為資料邊界。
- Auth 僅信任 `app_metadata.role` 作為 Admin 依據，redirect 僅允許站內安全路徑，登入與密碼復原錯誤不回傳 provider 細節。
- API 輸入經 Zod 驗證，錯誤為一致的安全碼；AI Key 為 server-only，遠端失敗會使用本地引擎回退；localStorage 不存放雲端憑證。

## 重現驗證

- `pnpm install --frozen-lockfile`：通過。
- `pnpm seed:supabase`：SAFE，知識 56、案例 30、無重複或無效資料、遠端寫入 0。
- `pnpm test`：448 passed，0 failed。
- `pnpm test:e2e`：27 passed，0 failed。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：通過。
- Protected Preview：PASS；健康檢查、公開內容、安全回應，以及未登入 cloud/Admin API 的 401 邊界均通過。

## 依賴風險判定

`pnpm audit --prod` 仍列出 7 high、6 moderate 的已知上游公告；未達 P0/P1，列為 P2 維護項。

- 受影響的 Next.js 版本為 16.2.10，但本專案沒有 i18n 設定、Server Actions、自訂 server 或 rewrites；因此公告所需的 Proxy bypass、Server Action 與 rewrite 攻擊入口不存在。
- 專案未在應用程式中使用 `next/image`，也不在執行期處理使用者可控制 CSS；因此 Sharp/PostCSS 公告所需入口不存在。
- 下一個維護週期仍應升級 Next.js 至至少 16.2.11，並一併更新其 transitive PostCSS/Sharp 依賴後重新執行完整驗證。

Supabase 已公告新的 public schema 資料表不會自動暴露於 Data API；目前專案採明確 GRANT 與 RLS，因此與此平台變更相容。[Supabase changelog](https://supabase.com/changelog?tags=data+apis)

## 封板判定

- P0：0
- P1：0
- P2：1（上游依賴升級維護）
- Production：未修改，Production cloud credentials 與 feature flags 仍未設定。
- 建議：可將 Draft PR #3 改為 Ready for review；合併前不應再擴張範圍，並在後續維護 PR 完成 Next.js 與 transitive dependency 升級。
